# Pytest Unit Tests

Unit-testing basics with **pytest**: a small math module and its full test suite.

## Files
- `simple_math.py` — functions under test
- `test_simple_math.py` — pytest test cases

## Run
```bash
pip install pytest
pytest -v
```
